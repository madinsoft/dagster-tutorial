telemetry:
  enabled: false
storage:
  class: dagster_postgres.PostgresInstance
  config:
    postgres_db:
      username: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      hostname: ${POSTGRES_HOST}
      db_name: ${POSTGRES_DB}
      port: ${POSTGRES_PORT}
      params: {}